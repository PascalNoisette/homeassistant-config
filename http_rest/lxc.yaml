- resource: !secret LXC_HOOK_URL
  scan_interval: 60
  headers:
    Pragma: >
      no-cache
  sensor:
    - name: "first memory usage"
      value_template: "{{ value_json.first.memory.usage }}"
      unit_of_measurement: B
    - name: "first memory swap_usage"
      value_template: "{{ value_json.first.memory.swap_usage }}"
      unit_of_measurement: B
    - name: "first network eth0 bytes_sent"
      value_template: "{{ value_json.first.network.eth0.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "first network eth0 bytes_received"
      value_template: "{{ value_json.first.network.eth0.counters.bytes_received }}"
      unit_of_measurement: B
    - name: "first network eth1 bytes_sent"
      value_template: "{{ value_json.first.network.eth1.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "first network eth1 bytes_received"
      value_template: "{{ value_json.first.network.eth1.counters.bytes_received }}"
      unit_of_measurement: B

    - name: "second memory usage"
      value_template: "{{ value_json.second.memory.usage }}"
      unit_of_measurement: B
    - name: "second memory swap_usage"
      value_template: "{{ value_json.second.memory.swap_usage }}"
      unit_of_measurement: B
    - name: "second network eth0 bytes_sent"
      value_template: "{{ value_json.second.network.eth0.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "second network eth0 bytes_received"
      value_template: "{{ value_json.second.network.eth0.counters.bytes_received }}"
      unit_of_measurement: B
    - name: "second network eth1 bytes_sent"
      value_template: "{{ value_json.second.network.eth1.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "second network eth1 bytes_received"
      value_template: "{{ value_json.second.network.eth1.counters.bytes_received }}"
      unit_of_measurement: B


    - name: "tvhead memory usage"
      value_template: "{{ value_json.tvhead.memory.usage }}"
      unit_of_measurement: B
    - name: "tvhead memory swap_usage"
      value_template: "{{ value_json.tvhead.memory.swap_usage }}"
      unit_of_measurement: B
    - name: "tvhead network eth0 bytes_sent"
      value_template: "{{ value_json.tvhead.network.eth0.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "tvhead network eth0 bytes_received"
      value_template: "{{ value_json.tvhead.network.eth0.counters.bytes_received }}"
      unit_of_measurement: B
    - name: "tvhead network eth1 bytes_sent"
      value_template: "{{ value_json.tvhead.network.eth1.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "tvhead network eth1 bytes_received"
      value_template: "{{ value_json.tvhead.network.eth1.counters.bytes_received }}"
      unit_of_measurement: B


    - name: "hass memory usage"
      value_template: "{{ value_json.hass.memory.usage }}"
      unit_of_measurement: B
    - name: "hass memory swap_usage"
      value_template: "{{ value_json.hass.memory.swap_usage }}"
      unit_of_measurement: B
    - name: "hass network eth0 bytes_sent"
      value_template: "{{ value_json.hass.network.eth0.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "hass network eth0 bytes_received"
      value_template: "{{ value_json.hass.network.eth0.counters.bytes_received }}"
      unit_of_measurement: B
    - name: "hass network eth1 bytes_sent"
      value_template: "{{ value_json.hass.network.eth1.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "hass network eth1 bytes_received"
      value_template: "{{ value_json.hass.network.eth1.counters.bytes_received }}"
      unit_of_measurement: B

    - name: "jitsi memory usage"
      value_template: "{{ value_json.jitsi.memory.usage }}"
      unit_of_measurement: B
    - name: "jitsi memory swap_usage"
      value_template: "{{ value_json.jitsi.memory.swap_usage }}"
      unit_of_measurement: B
    - name: "jitsi network eth0 bytes_sent"
      value_template: "{{ value_json.jitsi.network.eth0.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "jitsi network eth0 bytes_received"
      value_template: "{{ value_json.jitsi.network.eth0.counters.bytes_received }}"
      unit_of_measurement: B
    - name: "jitsi network eth1 bytes_sent"
      value_template: "{{ value_json.jitsi.network.eth1.counters.bytes_sent }}"
      unit_of_measurement: B
    - name: "jitsi network eth1 bytes_received"
      value_template: "{{ value_json.jitsi.network.eth1.counters.bytes_received }}"
      unit_of_measurement: B
