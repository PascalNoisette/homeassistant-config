reset_dry_date:
  alias: "Reset dry date"
  fields:
    entity_id:
      name: Entity Target Id
      required: true
  sequence:
  - service: input_datetime.set_datetime
    target:
      entity_id: "{{ entity_id }}"
    data:
      datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"
